<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.gameconsign">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launchergc"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launchergc_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginPenjualActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".RegistPenjualActivity" />
        <activity android:name=".RegistPembeliActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginPembeliActivity" />
        <activity android:name=".MenuPenjualActivity" />
        <activity android:name=".MenuPembeliActivity"></activity>
    </application>

</manifest>